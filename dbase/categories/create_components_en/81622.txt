{
  "title": " Adding containers to an array",
  "text": "Next, let's look at the dynamic creation of new page containers in the pages array.\r\n To do this, we will create a new component - the `create_page` form, in which we will create new containers:\r\n \r\n ```html\r\n<!-- created a new create_page component -->\r\n<form data-create_page=\"container\" style=\"border: 1px solid blue; padding: 10px; margin: 10px;\"> \r\n \r\n <div class=\"form-group\">\r\n <label for=\"container_text\">record text</label>\r\n\r\n<textarea data-create_page-text=\"inputvalue\" name=\"container_text\" id=\"container_text\" rows=\"1\"></textarea>\r\n<!--text property with data type \"inputvalue\" -->\r\n\r\n</div>\r\n \r\n <button data-create_page-create=\"click\">Create</button> \r\n <!-- create property with \"click\" data type -->\r\n\r\n</form>\r\n \r\n<div data-pages=\"array\" style=\"border: 1px solid red; padding: 10px;\">\r\n<!-- the pages array is unchanged (see the example above ##Creating an array) -->\r\n</div>\r\n \r\n ```\r\n \r\nNow let's create a new component - create_page in the application description:\r\n\r\n\r\n```javascript\r\n\r\nvar StateMap = {\r\n \r\n create_page: { // added a new component create_page\r\ncontainer: \"create_page\", // the name of the container matches the name of the component, since the container itself is a component \r\n props: [\"text\", \"create\"], // added two properties \r\n methods: {\r\ncreate: function(){ // a handler of the same name was added for the event property \r\n \r\n event.preventDefault(); // canceling the page reload \r\n \r\n var text = this.parent.props.text.getProp();\r\n// get the property data located in the same container\r\n \r\n this.rootLink.state[\"pages\"].add({paragraph: text});\r\n// creating a new page container in the pages component with the received form data \r\n                }\r\n     }\r\n},\r\npages: {\r\n<!-- the pages component is unchanged (see the example above ##Creating an array) -->\r\n          }\r\n}\r\n```\r\n\r\nSo we created the create_page component to create new page pages using the `.add()` array method.\r\n\r\nAfter creating a container, it gets an index depending on its position in the array, then when adding new containers to the beginning of the array or deleting previous containers, the index field of the container will also be updated and will contain up-to-date information."
}