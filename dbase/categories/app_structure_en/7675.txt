{
  "title": "The structure of the HTMLixArray array",
  "text": "Array Fields\r\n\r\n* `data` - contains all the containers of the array, the ordinal number of the container in the array matches the index field of the container, after adding or removing the container from the array, the index of other adjacent containers may change;\r\n* `htmlLink` - html link to the array;\r\n* `index` - null for an array;\r\n* `pathToComponent` - name of the component;\r\n* `RenderType` - the display type \"array\" can also be \"virtual-array\" for a virtual array;\r\n* `rootLink` - link to the root object;\r\n* `selector` - a clarifying selector for searching containers relative to the array tag, for example \"div:last-of-type\" - will search for the last div inside <div data-pages=\"array\">\r\n* `TemplateData` - a template for creating a new container for this array, the first one is taken from the array if specify the container type \"template\", then a copy is made from it during initialization, and the template is then removed from the array;\r\n* `type` - type \"array\";\r\n\r\nAlso, if the array is placed in a property with the \"render-variant\" type, there may be an additional field here:\r\n\r\n* `renderParent`- access to a property from an array;\r\n\r\nIf the array uses auxiliary methods with the \"aux\" type:\r\n\r\n* `methods` - access to all auxiliary methods"
}