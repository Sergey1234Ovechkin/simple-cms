{
  "title": "Container Structure Container",
  "text": "Container Fields\r\n\r\n* `htmlLink` - link to the Html object;\r\n* `index` - the ordinal number of the container in the array, **null** - if the container is not placed in the array, but is a component;\r\n* `name` - the name of the container in the application description;\r\n* `pathToComponent` - the name of the component, may match the name of the container if the container itself is a component (not placed in an array);\r\n* `props` - object with properties (Prop);\r\n* `RenderType` - container type \"container-outer\" says that the container is a component, \"container-inner\" - that it is in an array; \r\n* `rootLink` - link to the root element (application instance);\r\n* `type` - \"container\" - container object type (we specified it in the div element);\r\n\r\nThere may also be additional fields here.\r\n\r\n\r\nif the container is in a property with the type `group`:\r\n\r\n* `groupParent` - access from the container to the property;\r\n* `groupId` - index of the container in the group;\r\n\r\n\r\nif the container is in a property with the type `render-variant`:\r\n\r\n* `renderParent` - access from the container to the property\r\n\r\nif the container uses auxiliary methods with the \"aux\" data type:\r\n\r\n* `methods` - access to all auxiliary methods"
}