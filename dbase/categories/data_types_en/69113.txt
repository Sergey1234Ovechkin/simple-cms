{
  "title": "getProp()",
  "text": "Let's look at how the `getProp()` method works for the main types of properties:\r\n\r\n* `text` - text data - returns this.htmlLink.textContent;\r\n* `html` - markup - returns this.htmlLink.innerHTML;\r\n* `inputvalue`, `select` - returns form data - this.htmlLink.value;\r\n* `checkbox`, `radio` - checkboxes and radio returns tru or false - this.htmlLink.checked;\r\n* `class` - returns an array with classes;\r\n\r\n\r\n* `render-variant` - if the container is displayed in the property, calls the method.getAllProps(), calls the method for the array.GetAll(),\r\nthereby retrieves the values of all properties from the displayed element, if passed by the parameter MapObject={key1: \"\", key2: {ke1:\"\"} } `getProp(MapObject)`,\r\nreturns properties only matching the names with key, so you can get an expanded object with all property values, or with all the necessary property values from the displayed element;\r\n\r\n* `group` - returns an array with objects whose keys will be the names of properties, and the values\r\nobtained from these properties on each container works based on the getAllProps() method,\r\nif passed by the MapObject={key1: \"\", key2: {ke1: \"\"} }` parameter.getProp(MapObject)`,\r\nwill return properties that only match the names of the key, so you can get an expanded object with all the property values or with all the necessary property values from child containers;\r\n\r\n \r\n* `href`, `src`, `id`, `style` and other attributes returns this attribute this.htmlLink.getAttribute(this.type);\r\n* for events not normally used, returns this.type;\r\n\r\ntype **data** - because in data with the date type, there are any data after the = sign, then to create this type\r\n, the name `data_name` is written after the name of the array or container, and then any data, for example: `data-page-data_page_id`=\"any data\",\r\nhere, the data type is determined by the name of the property. it should always begin with the word data after the name of the container or array.\r\n\r\n* `data` - returns this.htmlLink.dataset[ this.parent.name + this.propName ] that is, the data after the \"=\" sign;"
}